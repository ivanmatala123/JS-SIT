let greetings = "Hello world "
let currentNum;
let currentOperation;
let previousNum;

function addnum(num){
    currentNum += num;
    document.getElementById("test") = `${previousNum} + ${currentOperation} + ${currentNum}`;
}

function addoperand(ope){
    if(currentNum === "") {return;}
    if(previousNum !== ""){
        
    }


}


function calc(){

}